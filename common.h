#pragma once
#include <vector>
#include <string>

std::vector<std::string> handle_inputs(const std::string &prefix);
std::string handle_input1(const std::string &prefix);
std::vector<std::string> split_str(const std::string &s, char delim = ' ');
